<Page 
    x:Class="AstralBattles.Views.ViaNetworkGameModes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModels="clr-namespace:AstralBattles.ViewModels" FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    >

    <Page.DataContext>
        <ViewModels:ViaNetworkGameModesViewModel/>
    </Page.DataContext>


    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock x:Name="PageTitle" Label="network" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <StackPanel>

                <HyperlinkButton Command="{Binding HostAServer}" 
                             Content="{Binding Resources.CommonResources.HostAServer}" 
                             Margin="0,3,0,0" 
                             Style="{StaticResource HyperlinkButtonStyle}"/>
                <TextBlock Foreground="Gray" 
                       Margin="15,-5,0,20" 
                       Label="{Binding Resources.CommonResources.HostAServerTip}"/>



                <HyperlinkButton Command="{Binding JoinToServer}" 
                             Content="{Binding Resources.CommonResources.JoinToServer}" 
                             Margin="0,3,0,0" 
                             Style="{StaticResource HyperlinkButtonStyle}"/>
                <TextBlock Foreground="Gray" 
                       Margin="15,-5,0,20" 
                       Label="{Binding Resources.CommonResources.JoinToServerTip}"/>


            </StackPanel>



        </Grid>
    </Grid>

    <!--Sample code showing usage of ApplicationBar-->
    <!--<Page.ApplicationBar>
        <CommandBar IsVisible="True" IsMenuEnabled="True">
            <CommandBarIconButton Icon="/Images/appbar_button1.png" Label="Button 1"/>
            <CommandBarIconButton Icon="/Images/appbar_button2.png" Label="Button 2"/>
            <CommandBar.MenuItems>
                <CommandBarMenuItem Label="MenuItem 1"/>
                <CommandBarMenuItem Label="MenuItem 2"/>
            </CommandBar.MenuItems>
        </CommandBar>
    </Page.ApplicationBar>-->

</Page>
